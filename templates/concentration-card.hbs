<div class="concentration-card">
  <ul class="dnd5e chat-card concentration-list concentration-effects-list">
    {{#each effects}}
    <li class="card-header" data-effect-uuid="{{uuid}}" {{#if @root/isGM}}draggable="true" {{/if}}>
      <img class="token-image" src="{{data.icon}}" title="{{data.label}}" width="36" height="36">

      <h3>{{data.label}}</h3>
      <div class="status-chip">
        <span>{{duration.label}}</span>
        <i class="fas fa-hourglass-half"></i>
      </div>
    </li>
    {{/each}}
  </ul>

  {{#if targetedTokens}}
  <details>
    <summary class="concentration-combatant-summary">
      <span class="concentration-combatant-summary-label">
        {{localize "concentration-5e.TARGETED"}}
      </span>
      <button type="button" class="concentration-apply" data-action="apply-all-effects-to-all" title="{{localize "concentration-5e.APPLY_ALL_TO_ALL"}}">
        <i class="fas fa-check-square"></i>
      </button>
    </summary>
    
    <ul class="dnd5e chat-card concentration-list concentration-combatant-list">
      {{#each targetedTokens}}
      <li class="card-header" data-token-id="{{id}}">
        <img class="token-image" src="{{data.img}}" title="{{data.name}}" width="36" height="36" style="transform: rotate({{data.rotation}}deg);">

        <h3>{{data.name}}</h3>

        <button type="button" class="concentration-apply" data-action="apply-all-effects" title="{{localize "concentration-5e.APPLY_ALL"}}"><i class="fas fa-check-square"></i></button>
      </li>
      {{/each}}
    </ul>
  </details>
  {{/if}}
</div>